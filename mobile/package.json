{
  "name": "budget-app-mobile",
  "version": "1.0.0",
  "description": "Budget App Mobile (Android)",
  "main": "index.js",
  "scripts": {
    "android": "cap open android",
    "build": "npm run build:web && cap sync",
    "build:web": "cd ../frontend && npm run build",
    "sync": "cap sync",
    "copy": "cap copy",
    "update": "cap update"
  },
  "keywords": [
    "capacitor",
    "android",
    "budget",
    "mobile"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.2.0",
    "@capacitor/haptics": "^6.0.1",
    "@capacitor/keyboard": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
